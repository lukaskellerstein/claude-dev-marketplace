{
  "mcpServers": {
    "docs-index": {
      "command": "python",
      "args": ["../../mcp-servers/docs-index/server.py"],
      "description": "Curated documentation index for infrastructure topics"
    },
    "fetch": {
      "command": "uvx",
      "args": ["mcp-server-fetch"],
      "description": "Web fetching for documentation URLs from docs-index"
    },
    "gcloud": {
      "command": "npx",
      "args": ["-y", "@google-cloud/gcloud-mcp"],
      "description": "Google Cloud CLI integration for infrastructure management"
    },
    "gcloudstorage": {
      "command": "npx",
      "args": ["-y", "@google-cloud/storage-mcp"],
      "description": "Google Cloud Storage management"
    },
    "terraform": {
      "command": "docker",
      "args": ["run", "-i", "--rm", "hashicorp/terraform-mcp-server:0.2.3"],
      "description": "Terraform infrastructure as code execution and management"
    }
  }
}
