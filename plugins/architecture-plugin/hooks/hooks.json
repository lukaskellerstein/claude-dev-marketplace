{
  "user-prompt-submit-hook": {
    "description": "Automatically trigger architecture skills when relevant",
    "commands": [
      "# Check if working with microservices or architecture",
      "if echo \"$PROMPT\" | grep -qiE '(microservice|service|architecture|pattern|design|refactor)'; then",
      "  echo 'Architecture context detected. Skills may be auto-invoked.'",
      "fi"
    ]
  },
  "pre-file-write-hook": {
    "description": "Validate architectural decisions before writing files",
    "commands": [
      "# Validate service boundaries when creating new services",
      "if echo \"$FILE_PATH\" | grep -qE 'services?/.*\\.(ts|js|go|py)$'; then",
      "  echo 'Service file detected. Consider running service-boundaries skill.'",
      "fi"
    ]
  }
}
